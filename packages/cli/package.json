{
  "name": "truspec",
  "version": "0.0.0",
  "description": "Local-first, spec-synced, agent-native API client — CLI.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "truspec": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@truspec/core": "workspace:*"
  }
}
